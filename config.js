/* =====================================================================
   オンライン全国ランキング設定
   バックエンドは Cloudflare D1（同一オリジンの /api/scores 経由）。
   APIキーの類は一切不要・ブラウザに出さない（サーバ側 functions/api/scores.js が処理）。
   ※ 旧Supabase構成の url / anonKey は不要になったため削除した。
   ===================================================================== */
window.RANKING = {
  topN: 500,   // 上位500位まで取得（ランキング画面はスクロールで全件見られる）
};
